import React, { useEffect, useMemo, useState } from "react";
import { CheckCircle2, ClipboardList, Database, FileSpreadsheet, RefreshCw, Search, ShieldCheck, Warehouse } from "lucide-react";
import { generateOperationalIds, type MerchantMaster, type PaymentMethod } from "@/lib/enterpriseWorkflow";
import { loadLiveMasterDataSnapshot } from "@/lib/liveMasterData";

type QueueStatus = "READY" | "CHECKING" | "WAREHOUSE READY" | "ERROR";
type EntryRow = { pickupId: string; deliverId: string; invoiceNo: string; waybillNo: string; merchantId: string; merchantName: string; merchantCode: string; parcelCount: number; payment: PaymentMethod; codAmount: number; status: QueueStatus };
const warehouseColumns = ["Scan Date", "Scan Time", "Warehouse Branch", "Operator", "Pickup ID", "Deliver ID", "Invoice No", "Waybill No", "Merchant Code", "Expected Parcel Count", "Scanned Parcel Count", "Current Status", "Bag Code", "Validation Status"];
const dataEntryColumns = ["Row No", "Requester Type", "Merchant ID", "Merchant Code", "Merchant Name", "Pickup Date", "Pickup Time", "Pickup Parcels", "Pickup ID", "Deliver ID", "Invoice No", "Waybill No", "Recipient Name", "Recipient Phone", "Delivery Address", "Payment Method", "COD Amount", "Service Type", "Priority", "Upload Status", "API Message"];
function buildRow(merchant: MerchantMaster, pickupDate: string, parcelCount: number, status: QueueStatus): EntryRow { return { ...generateOperationalIds(pickupDate, merchant.code, parcelCount), merchantId: merchant.id, merchantName: merchant.name, merchantCode: merchant.code, parcelCount, payment: merchant.paymentMethod, codAmount: 0, status }; }
export default function DataEntryPortalPage() {
  const [merchants, setMerchants] = useState<MerchantMaster[]>([]);
  const [merchantId, setMerchantId] = useState("");
  const [pickupDate, setPickupDate] = useState("2026-05-25");
  const [parcelCount, setParcelCount] = useState("15");
  const [query, setQuery] = useState("");
  const [rows, setRows] = useState<EntryRow[]>([]);
  const [loadingMaster, setLoadingMaster] = useState(false);
  const [notice, setNotice] = useState("Data Entry is loading the synchronized master-data snapshot.");
  async function syncMasterData() { setLoadingMaster(true); const snapshot = await loadLiveMasterDataSnapshot(); setMerchants(snapshot.merchants); setMerchantId((current) => snapshot.merchants.some((item) => item.id === current) ? current : snapshot.merchants[0]?.id ?? ""); setNotice(`Master data synchronized. ${snapshot.merchants.length} merchant records are available to Data Entry.`); setLoadingMaster(false); }
  useEffect(() => { void syncMasterData(); }, []);
  const merchant = useMemo(() => merchants.find((item) => item.id === merchantId) ?? merchants[0], [merchantId, merchants]);
  const count = Math.max(1, Number(parcelCount) || 1);
  const ids = useMemo(() => merchant ? generateOperationalIds(pickupDate, merchant.code, count) : generateOperationalIds(pickupDate, "XXX", count), [count, merchant, pickupDate]);
  const queue = useMemo(() => { const source = rows.length ? rows : merchants.slice(0, 2).map((item, index) => buildRow(item, "2026-05-25", 15, index === 0 ? "CHECKING" : "READY")); const q = query.trim().toLowerCase(); return q ? source.filter((row) => Object.values(row).join(" ").toLowerCase().includes(q)) : source; }, [merchants, query, rows]);
  function addRow() { if (!merchant) { setNotice("No merchant master record is selected."); return; } const next = buildRow(merchant, pickupDate, count, "READY"); setRows((prev) => [next, ...prev.filter((row) => row.pickupId !== next.pickupId)]); setNotice(`${next.pickupId} added from synchronized master data.`); }
  return <div className="min-h-screen bg-[#f6f8fc] p-8 text-slate-950"><section className="rounded-[32px] border border-slate-200 bg-white p-8 shadow-sm"><div className="flex items-start justify-between gap-5"><div><p className="text-sm font-black uppercase tracking-[0.24em] text-slate-400">Britium Enterprise Portal</p><h1 className="mt-3 text-5xl font-black tracking-tight">Data Entry Dashboard</h1><p className="mt-4 max-w-4xl text-lg font-semibold leading-8 text-slate-500">Data Entry uses the same live master-data snapshot as Customer Service, Supervisor, Warehouse, and Operations.</p></div><button type="button" onClick={syncMasterData} className="inline-flex items-center gap-2 rounded-2xl border border-slate-200 bg-white px-5 py-3 font-black shadow-sm hover:bg-slate-50"><RefreshCw size={18} className={loadingMaster ? "animate-spin" : ""} /> Sync Master Data</button></div><div className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-4"><Module title="Merchant Master Data" value={`${merchants.length} synchronized records`} icon={<Database size={20} />} /><Module title="Tariff Master Data" value="Master tariff fields auto-fill" icon={<FileSpreadsheet size={20} />} /><Module title="Employee Master Data" value="Shared with assignment modules" icon={<ShieldCheck size={20} />} /><Module title="Historical Entry Support" value={`${queue.length} synchronized examples`} icon={<ClipboardList size={20} />} /></div><div className="mt-8 rounded-2xl border border-emerald-100 bg-emerald-50 p-5 text-base font-bold text-emerald-700"><CheckCircle2 className="mr-2 inline" size={18} />{notice}</div></section><div className="mt-8 grid gap-8 xl:grid-cols-[1fr_520px]"><section className="rounded-[32px] border border-slate-200 bg-white p-8 shadow-sm"><h2 className="text-3xl font-black">Create Data Entry Record</h2><p className="mt-3 text-base font-semibold text-slate-500">Merchant lookup auto-fills code, phone, pickup address, township, city, tariff, payment, and ID fields from the shared master snapshot.</p><div className="mt-7 grid gap-5 md:grid-cols-3"><Field label="Merchant"><select value={merchantId} onChange={(event) => setMerchantId(event.target.value)} className="control">{merchants.map((item) => <option key={item.id} value={item.id}>{item.name} - {item.code}</option>)}</select></Field><Field label="Merchant Code"><input readOnly value={merchant?.code ?? ""} className="control locked" /></Field><Field label="Merchant Phone"><input readOnly value={merchant?.phone ?? ""} className="control locked" /></Field><Field label="Pickup Address"><input readOnly value={merchant?.pickupAddress ?? ""} className="control locked" /></Field><Field label="Township / City"><input readOnly value={merchant ? `${merchant.pickupTownship}, ${merchant.pickupCity}` : ""} className="control locked" /></Field><Field label="Tariff"><input readOnly value={merchant?.tariffProfile ?? ""} className="control locked" /></Field><Field label="Pickup Date"><input type="date" value={pickupDate} onChange={(event) => setPickupDate(event.target.value)} className="control" /></Field><Field label="Pickup Parcels"><input type="number" min="1" value={parcelCount} onChange={(event) => setParcelCount(event.target.value)} className="control" /></Field><Field label="Payment Method"><input readOnly value={merchant?.paymentMethod ?? ""} className="control locked" /></Field><Field label="Pickup ID"><input readOnly value={ids.pickupId} className="control idfield" /></Field><Field label="Deliver ID"><input readOnly value={ids.deliverId} className="control idfield" /></Field><Field label="Invoice No"><input readOnly value={ids.invoiceNo} className="control idfield" /></Field><Field label="Waybill No"><input readOnly value={ids.waybillNo} className="control idfield" /></Field></div><button type="button" onClick={addRow} className="mt-7 rounded-2xl bg-blue-700 px-7 py-4 font-black uppercase tracking-wider text-white hover:bg-blue-800">Add to Data Entry Queue</button></section><section className="rounded-[32px] border border-slate-200 bg-white p-8 shadow-sm"><h2 className="text-3xl font-black">Data Entry Queue</h2><div className="relative mt-6"><Search size={18} className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search ID, merchant, status..." className="h-14 w-full rounded-2xl border border-slate-200 pl-12 pr-4 font-semibold outline-none focus:border-blue-500" /></div><div className="mt-5 space-y-4">{queue.map((row) => <div key={row.pickupId} className="rounded-2xl border border-slate-200 p-5"><div className="font-mono text-lg font-black text-blue-700">{row.pickupId}</div><div className="mt-2 font-black">{row.merchantName} - {row.parcelCount} parcels</div><div className="mt-3 grid grid-cols-3 gap-2 text-xs font-bold text-slate-600"><Mini label="Deliver" value={row.deliverId} /><Mini label="Invoice" value={row.invoiceNo} /><Mini label="Waybill" value={row.waybillNo} /></div><div className="mt-3 rounded-xl bg-slate-50 px-3 py-2 text-xs font-black uppercase tracking-wider text-slate-500">{row.status}</div></div>)}</div></section></div><div className="mt-8 grid gap-8 xl:grid-cols-2"><TemplateCard title="Data Entry Template Format" columns={dataEntryColumns} /><TemplateCard title="Warehouse Template Format" columns={warehouseColumns} icon={<Warehouse size={20} />} /></div><style>{`.control{height:58px;width:100%;border-radius:18px;border:1px solid #e2e8f0;padding:0 18px;font-size:16px;font-weight:700;outline:none;background:white}.control:focus{border-color:#2563eb}.locked{background:#f1f5f9;color:#475569}.idfield{background:#eff6ff;color:#1d4ed8;font-family:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,monospace;font-weight:900}`}</style></div>;
}
function Module({ title, value, icon }: { title: string; value: string; icon: React.ReactNode }) { return <div className="rounded-2xl bg-slate-50 p-6"><div className="flex items-center gap-2 font-black">{icon}{title}</div><p className="mt-3 font-semibold text-slate-500">{value}</p></div>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block"><span className="mb-2 block text-sm font-black uppercase tracking-wider text-slate-500">{label}</span>{children}</label>; }
function Mini({ label, value }: { label: string; value: string }) { return <div className="rounded-xl bg-slate-50 p-3"><div className="text-[10px] uppercase tracking-wider text-slate-400">{label}</div><div className="mt-1 font-mono text-[11px] font-black">{value}</div></div>; }
function TemplateCard({ title, columns, icon }: { title: string; columns: string[]; icon?: React.ReactNode }) { return <section className="rounded-[32px] border border-slate-200 bg-white p-8 shadow-sm"><h2 className="flex items-center gap-2 text-2xl font-black">{icon}{title}</h2><div className="mt-5 grid gap-2 md:grid-cols-2">{columns.map((column, index) => <div key={column} className="rounded-xl bg-slate-50 px-4 py-3 text-sm font-bold text-slate-600"><span className="mr-2 text-slate-400">{index + 1}.</span>{column}</div>)}</div></section>; }
